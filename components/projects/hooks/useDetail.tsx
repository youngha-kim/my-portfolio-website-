const useDetail = (propsKeys : any, props : any) => {
  // console.log(propsKeys, props)

  // props를 돌면서 뎁스가 있으면 그 뎁스의 값들을 저장 

  const some : any = [propsKeys, props];
    return { some };
  }



export default useDetail