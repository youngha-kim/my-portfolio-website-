
const ReadMeDetail = (props: any) => {

  return (
    <>
      <div>{props.img}</div>
      <div>{props.title}</div>
      <div>{props.content}</div>
      <div>{props.description}</div>
    </>
  );
};

export default ReadMeDetail;
